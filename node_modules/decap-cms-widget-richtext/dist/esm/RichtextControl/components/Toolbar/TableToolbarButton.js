import React from 'react';
import { useEditorRef, useEditorSelector } from 'platejs/react';
import { TablePlugin } from '@platejs/table/react';
import ToolbarButton from './ToolbarButton';
import { jsx as ___EmotionJSX } from "@emotion/react";
function TableToolbarButton({
  label,
  icon,
  disabled
}) {
  const editor = useEditorRef();
  const isInTable = useEditorSelector(editor => !!editor.api.node({
    match: {
      type: TablePlugin.key
    }
  }), []);
  function handleClick() {
    if (!isInTable) {
      editor.tf.insert.table({
        rowCount: 2,
        colCount: 2
      });
    }
    editor.tf.focus();
  }
  return ___EmotionJSX(ToolbarButton, {
    label: label,
    icon: icon,
    onClick: handleClick,
    isActive: isInTable,
    disabled: disabled
  });
}
export default TableToolbarButton;